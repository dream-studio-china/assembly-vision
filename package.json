{
  "name": "assemblyvision-typescript",
  "private": true,
  "packageManager": "pnpm@10.12.4",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "vue-demi", "electron"]
  },
  "scripts": {
    "build": "pnpm -r --filter './packages/typescript/**' build",
    "test": "pnpm -r --filter './packages/typescript/**' test",
    "lint": "pnpm -r --filter './packages/typescript/**' lint"
  }
}
